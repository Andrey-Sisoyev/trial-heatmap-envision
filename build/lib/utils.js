// Generated by IcedCoffeeScript 1.6.3-g
(function() {
  var CircularJSON, moment, prettyjson;



  moment = require('moment');

  prettyjson = require('prettyjson');

  CircularJSON = require('circular-json');

  exports.formatDate = function(date, format) {
    if (format == null) {
      format = 'MM-DD-YYYY HH:mm:ss';
    }
    return moment(date).format(format);
  };

  exports.toStringTol = function(x) {
    if (x != null) {
      return '' + x;
    } else {
      return 'undefined';
    }
  };

  exports.setTimeout_ = function(to, cb) {
    return setTimeout(cb, to);
  };

  exports.setTimeout0 = function(cb) {
    return setTimeout(cb, 0);
  };

  exports.getRandomInt = function(min, max) {
    return Math.floor((Math.random() * (max - min + 1)) + min);
  };

  exports.safePrettyPrintC = function(what) {
    return prettyjson.render(JSON.parse(CircularJSON.stringify(what)));
  };

  exports.safePrint = function(what) {
    return JSON.stringify(JSON.parse(CircularJSON.stringify(what)));
  };

}).call(this);
